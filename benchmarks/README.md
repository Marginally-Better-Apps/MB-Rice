# Benchmarks

No performance measurements have been run. `results.json` records that status without invented latency or memory numbers. Before a release, run the workloads in the brief on a signed iPhone build and retain raw samples with device, OS, node count, appearance, build type and error counts.
